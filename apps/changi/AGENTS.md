# Changi Airport demo — cloud notes

- Dev: `pnpm dev:changi` (port **5176**)
- No env required; `.env.example` documents optional `VITE_*` vars
- `pnpm test` here is Vitest (safe at root). `pnpm shots` needs Playwright Chromium for CV screenshots
- Unofficial demo — not affiliated with Changi Airport Group
