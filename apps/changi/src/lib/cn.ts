/**
 * Class merger — re-exported from the shared `@demo/ui` package so every app
 * in the monorepo uses the exact same clsx + tailwind-merge implementation.
 */
export { cn, type ClassValue } from "@demo/ui/cn";
