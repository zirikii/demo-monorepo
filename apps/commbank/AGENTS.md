# CommBank demo — cloud notes

- Dev: `pnpm dev:commbank` (port **5179**)
- No env required; `.env.example` documents optional `VITE_*` vars
- `pnpm test` is Vitest (safe at root)
- Brand: prefer official logo via `scripts/fetch-brand-assets.sh`; fallback mark is committed when CDN is blocked
- Unofficial demo — not affiliated with Commonwealth Bank of Australia
