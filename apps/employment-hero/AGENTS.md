# Employment Hero demo — agent notes

- Stack: Vite + React 19 + Tailwind v4 + TypeScript. Port **5180**.
- Brand purple `#7622D7` is intentional (official Employment Hero colour).
- Mock auth accepts any credentials; sessions in `localStorage` under `employment-hero-demo-session`.
- Login page includes an intentional rendering defect used for demo bug-fix flows (similar to CommBank). Tracked in Jira project DR with labels `bug`, `employment-hero`, `low`.
- Do not hotlink employmenthero.com assets; use `public/brand/`.
